GEMINI_API_KEY=replace_with_your_gemini_authorization_key
# Optional override; defaults to the previously working model.
GEMINI_MODEL=gemini-3.6-flash
# Recommended for private/demo deployments. The browser will show a Basic Auth prompt.
JURISAI_ACCESS_PASSWORD=replace_with_a_long_random_password

